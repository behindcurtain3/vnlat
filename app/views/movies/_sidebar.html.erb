<div class="col-sm-3">
  <div class="list-group">
    <%= link_to t('movie.sidebar.home'), movie_path(@movie), class: 'list-group-item active' %>
    <%= link_to t('movie.sidebar.boxoffice'), boxoffice_movie_path(@movie), class: 'list-group-item' %>
  </div>
</div>
