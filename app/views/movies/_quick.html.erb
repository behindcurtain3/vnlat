<li>
    <div class="movie-item">
        <strong><%= link_to movie.title.upcase, movie_path(movie) %> <small>(<%= movie.year %>)</small></strong>
    </div>
    <div class="ratings">
        <%= render 'buttons', movie: movie, sm: true %>
    </div>
</li>
