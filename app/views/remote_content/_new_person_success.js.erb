// hide the old modal
$("#new-person-modal").modal('toggle');

// get the new html and show it
$(this).delay(500).queue(function() {
  $("#new-person-modal-container").html("<%= escape_javascript(render :partial => "remote_content/new_person_success", :formats => [:html]) %>");
  $("#new-person-modal-success").modal();
});
